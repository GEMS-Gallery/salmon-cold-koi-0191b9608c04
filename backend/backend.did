type Post = 
 record {
   author: text;
   body: text;
   category: text;
   id: nat;
   timestamp: int;
   title: text;
 };
service : {
  createPost: (text, text, text, text) -> (nat);
  getPosts: () -> (vec Post) query;
}
